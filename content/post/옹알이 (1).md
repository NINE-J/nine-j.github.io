---
publish: false
draft: true
title: 옹알이 (1)
description: programmers, Java
author: Nine
date: 2026-02-24 15:30:32
categories:
  - 알고리즘
  - 프로그래머스
tags:
  - algorithms
  - programmers
  - lv0
  - Java
  - 구현
  - 시뮬레이션
  - 문자열
---
## 📌문제 설명

머쓱이는 태어난 지 6개월 된 조카를 돌보고 있습니다. 조카는 아직 "aya", "ye", "woo", "ma" 네 가지 발음을 최대 한 번씩 사용해 조합한(이어 붙인) 발음밖에 하지 못합니다. 문자열 배열 `babbling`이 매개변수로 주어질 때, 머쓱이의 조카가 발음할 수 있는 단어의 개수를 return하도록 solution 함수를 완성해주세요.

## 📌제한사항

- 1 ≤ `babbling`의 길이 ≤ 100
- 1 ≤ `babbling[i]`의 길이 ≤ 15
- `babbling`의 각 문자열에서 "aya", "ye", "woo", "ma"는 각각 최대 한 번씩만 등장합니다.
    - 즉, 각 문자열의 가능한 모든 부분 문자열 중에서 "aya", "ye", "woo", "ma"가 한 번씩만 등장합니다.
- 문자열은 알파벳 소문자로만 이루어져 있습니다.

## 📌입출력 예

|babbling|result|
|---|---|
|["aya", "yee", "u", "maa", "wyeoo"]|1|
|["ayaye", "uuuma", "ye", "yemawoo", "ayaa"]|3|

## 📌입출력 예 설명

### 입출력 예 #1

- `["aya", "yee", "u", "maa", "wyeoo"]`에서 발음할 수 있는 것은 "aya"뿐입니다. 따라서 1을 return합니다.

### 입출력 예 #2

- `["ayaye", "uuuma", "ye", "yemawoo", "ayaa"]`에서 발음할 수 있는 것은 "aya" + "ye" = "ayaye", "ye", "ye" + "ma" + "woo" = "yemawoo"로 3개입니다. 따라서 3을 return합니다.

## 📌유의사항

- 네 가지를 붙여 만들 수 있는 발음 이외에는 어떤 발음도 할 수 없는 것으로 규정합니다. 예를 들어 "woowo"는 "woo"는 발음할 수 있지만 "wo"를 발음할 수 없기 때문에 할 수 없는 발음입니다.

>[!WARNING]
>※ 공지 - 2022년 10월 27일 문제 지문이 리뉴얼되었습니다. 기존에 제출한 코드가 통과하지 못할 수도 있습니다.

## 📌풀이

이 문제의 데이터 규모는 매우 작다고 볼 수 있다.

1. 배열 크기 ($N$): $1 \leq babbling \leq 100$
2. 문자열 길이($L$): $1 \leq babbling[i] \leq 15$
3. 전체 연산 횟수: 최악의 경우에도 $100 \times 15$ 수준의 연산으로 해결 가능하다.

### 의사코드 작성

1. `aya`, `ye`, `woo`, `ma` 주어진 4가지 발음의 단어를 조합하여 목표 문자열을 만들 수 있는가?
2. `babbling` 순회
    1. 아이템의 문자열에서 단어에 해당하는 문자열을 공백 `" "` 또는 특수문자로 치환. 단, 이때 빈 값`""`으로 치환하면 안 된다. `wyeoo`에서 `ye`를 제거하면 `woo`가 남게 되어 원래는 불가능한 발음이 가능한 것으로 오판될 수 있다.
    2. 치환 후 문자열이 비어 있는지 체크

### 기본 제공 코드

```java
class Solution {
    public int solution(String[] babbling) {
        int answer = 0;
        
         "aya", "ye", "woo", "ma"
        
        return answer;
    }
}
```

### 제출 코드

```java
class Solution {
    public int solution(String[] babbling) {
        int answer = 0;
        String[] words = {"aya", "ye", "woo", "ma"};
        
        for(String b : babbling) {
            for(String word: words) {
                b = b.replace(word, " ");
            }

            if(b.trim().isEmpty()) answer++;
        }
        
        return answer;
    }
}
```

---

## 📌회고

특정 문자열이 주어진 단어로 조합된 문자열인지 체크하는 방식엔 여러가지가 있을 것이다.

첨엔 아이템에 대해 포인트를 잡고 이동하면서 한 글자씩 체크하려고 했지만 복잡하게 생각하지 않아도 가능했다.

---

# 유형 파악 가이드

다양한 방식으로도 풀이가 가능하겠지만, 효율적인 유형 파악을 위한 간단한 가이드를 정리한다.

## 1. 컴퓨팅 사고력: 1초의 법칙

컴퓨터는 **1초에 약 $10^8$(1억) 번 연산**한다. 지문의 제약 사항($N$)을 보고 허용되는 시간 복잡도($\mathcal{O}$)를 즉시 판별하라.

| 데이터 규모 ($N$)       | 허용 시간 복잡도               | 권장 알고리즘                  |
| :----------------- | :---------------------- | :----------------------- |
| $N \le 20$         | $\mathcal{O}(2^N)$      | 브루트 포스, 백트래킹             |
| $N \le 500$        | $\mathcal{O}(N^3)$      | 플로이드-워셜, 3중 for문         |
| $N \le 2,000$      | $\mathcal{O}(N^2)$      | 이중 for문, 삽입 정렬           |
| $N \le 100,000$    | $\mathcal{O}(N \log N)$ | 정렬, 이분 탐색, 힙, 다익스트라      |
| $N \le 10,000,000$ | $\mathcal{O}(N)$        | 단일 for문, 슬라이딩 윈도우, 투 포인터 |
| $N > 10^7$         | $\mathcal{O}(\log N)$   | 이분 탐색 (Binary Search)    |

## 2. 알고리즘 스캐닝 맵

지문의 **목적**에 따라 최적의 경로를 선택하라.

### A. 최단 거리 및 탐색

- **가중치 없는 그래프:** `BFS` (Queue 사용)
- **양의 가중치 그래프:** `Dijkstra` (Priority Queue 사용)
    - 복잡도: $\mathcal{O}(E \log V)$
- **음의 가중치 포함:** `Bellman-Ford`
- **모든 정점 간 거리:** `Floyd-Warshall`
    - 복잡도: $\mathcal{O}(V^3)$

### B. 최적화 및 결정 문제

- **그리디(Greedy):** 현재의 최선이 전체의 최선임을 증명 가능할 때.
- **DP(Dynamic Programming):** 작은 문제의 해가 큰 문제에 재사용될 때 (Memoization).
- **파라메트릭 서치:** 최적화 문제를 "Yes/No" 결정 문제로 치환 가능할 때.